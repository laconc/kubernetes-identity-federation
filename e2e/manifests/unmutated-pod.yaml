apiVersion: v1
kind: Pod
metadata:
  name: unmutated-pod
  namespace: apps
spec:
  # Uses the default SA which has no CloudRoleBinding — webhook must not inject.
  serviceAccountName: default
  restartPolicy: Never
  containers:
    - name: sleep
      image: busybox:stable
      command: ["sh", "-c", "sleep 60"]
