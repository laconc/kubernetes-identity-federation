apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: apps
spec:
  serviceAccountName: test-app
  restartPolicy: Never
  containers:
    - name: sleep
      image: busybox:stable
      command: ["sh", "-c", "sleep 3600"]
